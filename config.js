// This is client side config only - don't put anything in here that shouldn't be public!
export const endpoint = `http://api.onepointfiver.com/api/graphql`;
export const prodEndpoint = `http://api.onepointfiver.com/api/graphql`;
export const perPage = 4;
